/** @type {import('next').NextConfig} */
export default {
  // The backend URL is read server-side only. It is never exposed to the browser,
  // so no NEXT_PUBLIC_ variable carries anything that identifies a practice.
  env: {},
};
